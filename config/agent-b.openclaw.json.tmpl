{
  "gateway": {
    "mode": "local",
    "bind": "custom",
    "customBindHost": "0.0.0.0",
    "port": 18889,
    "auth": { "mode": "token", "token": "dating-loopback-b" },
    "nodes": { "pairing": { "autoApproveCidrs": ["127.0.0.1/32", "::1/128"] } }
  },

  "models": {
    "providers": {
      "openai": {
        "baseUrl": "https://api.openai.com/v1",
        "apiKey": "${OPENAI_API_KEY}",
        "api": "openai-completions",
        "models": [{ "id": "gpt-4o-mini", "name": "GPT-4o mini" }]
      }
    }
  },

  "agents": {
    "defaults": {
      "model": { "primary": "openai/gpt-4o-mini" }
    }
  },

  "plugins": {
    "load": { "paths": ["/plugin"] },
    "entries": {
      "agent-dating": {
        "enabled": true,
        "config": {
          "moiMnemonic": "${AGENT_B_MOI_MNEMONIC}",
          "moiDerivationPath": "${MOI_DERIVATION_PATH}",
          "agentUrl": "${AGENT_B_URL}",
          "datingPeerOwner": "${AGENT_B_PEER_OWNER}",
          "useAgentBrain": true
        }
      },
      "browser": { "enabled": false },
      "canvas": { "enabled": false },
      "device-pair": { "enabled": false },
      "file-transfer": { "enabled": false },
      "memory-core": { "enabled": false },
      "phone-control": { "enabled": false },
      "talk-voice": { "enabled": false }
    }
  },

  "tools": {
    "allow": ["dating_register", "dating_discover", "dating_send", "dating_date", "dating_verdict"]
  }
}
