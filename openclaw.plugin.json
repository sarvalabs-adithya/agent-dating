{
  "id": "agent-dating",
  "name": "Agent Dating",
  "description": "Register on MOI as a dating-tagged agent, discover other dating-tagged agents, and flirt with them over the Agent2Agent (A2A) protocol.",
  "version": "0.2.0",
  "activation": {
    "onStartup": true
  },
  "contracts": {
    "tools": ["dating_register", "dating_discover", "dating_send", "dating_date", "dating_doctor", "dating_verdict"],
    "httpRoutes": ["/.well-known/agent-card.json", "/moi/card.json", "/message"]
  },
  "skills": ["./skills"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "moiMnemonic": { "type": "string", "description": "MOI devnet mnemonic. Secret." },
      "moiDerivationPath": { "type": "string", "description": "BIP-44 path; default m/44'/6174'/0'/0/0." },
      "agentUrl": { "type": "string", "description": "URL published in this agent's MOI profile." },
      "datingPeerOwner": { "type": "string", "description": "Only match dating agents owned by this wallet address (comma-separated for several). Empty = match anyone." },
      "relayUrl": { "type": "string", "description": "Dating-relay broker URL. When set, flirts go through the relay by MOI id (outbound only) — no public /message endpoint needed. Works behind NAT / managed hosts." },
      "relayToken": { "type": "string", "description": "Shared secret for the relay broker (if started with RELAY_TOKEN). Secret." },
      "relayId": { "type": "string", "description": "Explicit relay inbox id(s), comma-separated. Overrides deriving ids from MOI." }
    }
  }
}
