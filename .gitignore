node_modules/
package-lock.json
*.log
.DS_Store
.env
*.mnemonic
secrets/
# bootstrap output: rendered configs (contain devnet mnemonics) + live chat logs
runtime/
*.chat.jsonl

# saved dump of the served /app page (source of truth is APP_HTML in
# relay/broker.mjs — a committed copy would silently drift)
/app.html

# smoke-test temp state
.smoke/
.date/
.prove/
.authtest/
.baretest/
.a2aout/
.relayout/
relay-data/
