{
  "name": "agent-dating",
  "version": "0.2.0",
  "description": "OpenClaw plugin: MOI-registered dating agents that flirt with each other over Agent2Agent (A2A) protocol.",
  "type": "module",
  "openclaw": {
    "extensions": ["./src/index.ts"],
    "compat": {
      "pluginApi": ">=2026.6.9"
    }
  },
  "scripts": {
    "test": "node --test",
    "check:broker": "node --check relay/broker.mjs"
  },
  "dependencies": {
    "typebox": "1.1.39",
    "js-moi-sdk": "^0.7.0-rc15",
    "js-moi-agent-registry": "^0.1.1"
  },
  "engines": {
    "node": ">=22"
  }
}
